import UIKit
import SegmentedControl

let frame = CGRect(origin: CGPointZero, size: CGSize(width: 320.0, height: 40.0))
let segmentedControl = SegmentedControl(frame: frame)
segmentedControl.titles = ["Foo", "Bar", "Baz"]
